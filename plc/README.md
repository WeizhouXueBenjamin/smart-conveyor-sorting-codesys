# PLC Projects

The `CODESYS OPC UA` directory contains the Factory I/O CODESYS OPC UA sample used for initial communication testing.

The custom control logic will be added in a separate project after the communication path is verified.
